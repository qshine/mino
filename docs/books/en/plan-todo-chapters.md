# Mino Chapter Roadmap

This tutorial builds a terminal agent from scratch in Go using the OpenAI Responses API. The official OpenAI Go SDK handles API communication; Mino implements the interaction flow and the Agent loop introduced in Chapter 03. Each chapter follows one interaction: what the model receives, what the program does with the response, and how control or context reaches the next step. [Getting started](./getting-started.md) covers installation and API settings.

Chapters 01–05 are released, with code, bilingual lessons, and verification. Chapter 05 is available in **chapter-05**, application version **0.5.0**, released **2026-09-26**. Statuses distinguish **Released** and **Not started**. Chapters 06–08 remain planned.

| Chapter | Status | Title | What it covers |
| --- | --- | --- | --- |
| 01 | Released | [Your first terminal conversation](chapters/01-terminal-chat.md) | Send current input and the loaded identity in a streaming Responses request, display text deltas immediately, and check completion before returning to the prompt. Use mock tests to observe streaming, failure, and cancellation; use two questions to explain why this version's independent requests have no memory. |
| 02 | Released | [Keeping a conversation going with JSONL history](chapters/02-jsonl-history.md) | Save one conversation in `history.jsonl`, with a separate `turn_id` per turn and consecutive record sequence numbers. Restore completed exchanges at startup, retaining response output items for the next request while streaming answers immediately. Distinguish stored history from supplied context; verify failure exclusion, private files, a single-process lock, and recovery of incomplete final records. Release `chapter-02` (version `0.2.0`). |
| 03 | Released | [The first Agent loop with tools and Bash](chapters/03-tools-and-bash.md) | Separate CLI input, display, and confirmation from a synchronous Agent loop with one Session. Inject Bash through the shared tool contract, approve each operation, and return paired results. Save and sync complete responses before committing replay state; recover interrupted calls without re-execution. Bound execution time, output, and loop length. Tools live in `internal/tools/`. Release `chapter-03` (version `0.3.0`). |
| 04 | Released | [Managing multiple conversations with JSONL sessions](chapters/04-jsonl-sessions.md) | Keep one JSONL file per session, list and resume complete IDs, and restore the last selection. Use `/new` for empty context and `/clear` with exact-ID confirmation to clear one session. Migrate legacy history with a retained archive; recover tool results without re-execution. Verify isolated requests, durable selection, and failure boundaries. Release `chapter-04` (version `0.4.0`). |
| 05 | Released | [Context compaction with manual and automatic triggers](chapters/05-context-compaction.md) | Share one summary flow between `/compact` and automatic checks before every model request, including after tools. Default to a configurable 128,000-token window. Keep the latest two replayable turns and the complete active turn; persist summaries and coverage in version 3 records while retaining original history. Verify restart, session isolation, request budgets, and bounded failure with mock services. Release `chapter-05` (version `0.5.0`). |
| 06 | Not started | Skills: Load Task Knowledge When Needed | Discover skills in the project's `.agents/skills/` and the user's `~/.agents/skills/`. At startup and on `/new`, include skill names, descriptions, and paths in instructions. After selecting a skill, the model uses a tool to read its complete `SKILL.md`. Explain discovery and on-demand loading without inserting every skill's full contents into every request. |
| 07 | Not started | MCP: Connect External Tools | Connect Go to MCP services, discover tool names, descriptions, and parameter schemas, and convert them into Responses API tool definitions. Forward calls to the corresponding service and return results to the model. Reuse the existing agent loop and handle naming conflicts, connection failures, and tool errors. |
| 08 | Not started | Guardrails: Keep the Agent Within Its Authorization | **Context controls:** explain jailbreaks and prompt injection; distinguish trusted instructions from external file, tool, and MCP content; preserve sources and trust boundaries during summarization. **Execution controls:** check concrete operations and arguments in code, then allow, reject, or request authorization; dangerous operations such as deletion require authorization bound to what will actually run. **Data access controls:** constrain where files and data can be read, written, or deleted; protect sensitive information and check path escapes and symbolic links. Explain the bypass risks of a general Bash tool and the roles of operating-system permissions and sandboxes. Verify behavior with injection attempts, denied authorization, and out-of-bounds access. |

Basic protections arrive alongside the capabilities that need them. Chapter 08 brings those controls together into a consistent permissions policy. Prompts and injection detection can reduce risk, but the program and execution environment must enforce actual permissions. Checking only command keywords such as `rm` is not a sufficient policy.

## Chapter 05 implementation boundary

Chapter 05 combines the former manual and automatic compaction chapters. The [lesson](./chapters/05-context-compaction.md) explains the shared interaction and its mock request checks; [setup](./getting-started.md#configure-the-context-window) covers `context_window`, whose omitted or zero value uses **128,000 tokens** without model-metadata discovery.

The local estimate uses serialized UTF-8 bytes plus framing, reserves output space, and triggers above 80% of the input budget. It is not an exact tokenizer. Compaction is limited to one tool-free summary request per attempt and one automatic attempt per user turn. Oversized material, failed or non-shrinking summaries, and context still above the hard budget produce errors; there is no recursive or batched compaction. Original records stay on disk, so compaction is not storage cleanup. Live model quality and provider compatibility remain outside the mock tests' evidence.
