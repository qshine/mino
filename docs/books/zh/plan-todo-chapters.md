# Mino 章节规划与完成状态

本教程使用 Go 和 OpenAI Responses API，从零实现一个终端 Agent。OpenAI 官方 Go SDK 负责 API 通信，Mino 实现交互流程，以及后续章节的 Agent 循环。每章追踪一次交互：模型收到什么，程序怎样处理响应，控制或上下文如何进入下一步。[准备与安装](./getting-started.md)集中说明安装和 API 设置。

第 01、02 章已完成代码、说明和本地验证。重新发布的 `v0.1.0` 对应第一章；第二章的目标版本为 `v0.2.0`，目前**尚未发布（Unreleased）**，需要使用包含第二章实现的源码体验历史功能。第三章及以后尚未开始。状态统一使用「未开始」「进行中」「已完成」；章节说明、对应代码和必要验证全部完成后，才能标记为「已完成」，这个状态不代表应用版本已经发布。

| 序号 | 完成状态 | 章节名称 | 核心内容介绍 |
| --- | --- | --- | --- |
| 01 | 已完成 | [与模型对话：第一个终端程序](chapters/01-terminal-chat.md) | 将当前输入和已加载的身份组成流式 Responses 请求，立即显示文本增量，检查完成后再回到输入提示。用模拟测试观察流式显示、失败和取消，用两问实验解释这个版本的独立请求为什么没有记忆。 |
| 02 | 已完成 | [让对话连续起来：JSONL 对话历史](chapters/02-jsonl-history.md) | 在 `history.jsonl` 中保存一个会话，每轮使用独立的 `turn_id`，记录序号连续。启动时恢复已完成的问答，为后续请求保留响应输出项，回答仍即时流式显示。区分文件历史与实际提供的上下文，验证失败排除、私有文件、单进程锁和不完整尾部恢复。目标版本 `v0.2.0`，尚未发布。 |
| 03 | 未开始 | 第一个 Agent 循环：工具调用与 Bash | 通过 `tools` 注册 Bash 工具的名称、描述和参数结构，实现「模型请求工具 → Go 执行 → 结果交回模型 → 继续调用或回答」的循环；保留工具调用与结果的关联，展示执行过程。同步引入执行前确认、超时、输出长度限制和最大循环次数。 |
| 04 | 未开始 | 管理多个会话：JSONL 与 Session | 每个会话使用独立的 `~/.mino/sessions/<session_id>.jsonl` 文件，由文件名承载会话 ID。保存对话条目及配对的工具调用与结果，支持恢复指定会话。通过 `/new` 创建新文件并保留旧会话，通过 `/clear` 清空当前会话历史，以及后续章节引入的摘要；验证会话隔离，区分持久化记录与实际提供的上下文。 |
| 05 | 未开始 | 手动压缩上下文：`/compact` | 将较早的交互整理为摘要，保留近期完整交互和原始历史记录；摘要涵盖用户目标、约束、已完成事项和待办事项。避免拆散工具调用与结果，压缩失败时保留原上下文，并演示压缩前后的信息保留与丢失。 |
| 06 | 未开始 | 自动管理上下文：预算与压缩触发 | 设置上下文预算，考虑系统指令、工具定义、历史与工具结果的占用，为模型输出预留空间；接近阈值时自动复用压缩流程。处理单次输入或工具结果过大、压缩失败及压缩后仍超限的情况。 |
| 07 | 未开始 | 支持 Skills：按需加载任务知识 | 扫描项目 `.agents/skills/` 与用户 `~/.agents/skills/`；启动和 `/new` 时将技能名称、简介及路径注入系统指令。模型选择技能后，通过工具读取完整 `SKILL.md`，理解技能发现与按需加载，避免一次加载所有正文。 |
| 08 | 未开始 | 支持 MCP：接入外部工具 | 由 Go 程序连接 MCP 服务，发现工具的名称、描述和参数结构，转换为 Responses API 的工具定义；将调用转发给对应服务，再把结果交回模型。复用已有 Agent 循环，处理命名冲突、连接失败和工具错误。 |
| 09 | 未开始 | 安全防护 Guardrails：让 Agent 在授权范围内行动 | **上下文安全防护**：讲解越狱与提示注入，区分可信指令和文件、工具、MCP 返回的外部内容，避免摘要丢失来源与信任边界。**工具执行安全防护**：在程序中检查具体操作与参数，返回允许、拒绝或请求授权；删除等危险操作须获用户授权，授权绑定实际执行内容。**数据访问安全防护**：约束文件与数据的读取、写入、删除范围，保护敏感信息，检查路径越界与符号链接；说明通用 Bash 的绕过风险及操作系统权限、沙箱的作用。用注入、拒绝授权和越界访问案例验证安全防护。 |

基础保护随相应能力一起引入，第 09 章负责系统梳理并统一权限策略。提示词和注入检测只能降低风险，实际权限必须由程序与执行环境约束；不能只凭 `rm` 等命令关键词判断安全性。
